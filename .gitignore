__pycache__/
*.pyc
*.log
.DS_Store
nohup.out

# GPAW heavy outputs
/data/outputs/gpaw_calculations/

# AdsorbML results and UMA-relaxed structures (kept locally, not in git)
/data/adsorbml_results/
/data/uma_relaxed/

# Snapshots pulled back off the cluster. Heavy, and superseded by every rerun --
# they were only in .git/info/exclude, so a fresh clone had nothing stopping a
# `git add -A` from committing them.
/partial_results/
/perun_logs/

# Docs (keep locally, ignore in git)
/docs/

# Optional: Jupyter
.ipynb_checkpoints/
